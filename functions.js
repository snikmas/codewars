//1
function classic (parameter) {
  return 0;
};

let arrayFunction = (par1, par2) => {
  return par1 * par2;
}
let a = ((paer) => {
  
})

let text = 'doggydog'.split('');
text.forEach((x => console.log(x, typeof(x), '-')));
